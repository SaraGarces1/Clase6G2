# %x Para el tiempo, la fecha

self.__felinos = {}
self.__caninos = {}
f = len(felinos)
c = len(caninos)


if f >= 7 :
    print(f"No se permite ingresar más mascotas, cupo {f} l1leno")
elif c >= 7 :
    print(f"No se permite ingresar más mascotas, cupo {c} l1leno")
else:
    continue

#fecha
from datetime import date
from datetime import datetime


       